git_url(ENV["SKYMATIC_MATCH_GIT_URL"])
app_identifier([ENV["BUNDLE_IDENTIFIER"], "#{ENV["BUNDLE_IDENTIFIER"]}.fileprovider", "#{ENV["BUNDLE_IDENTIFIER"]}.fileprovider-ui", "#{ENV["BUNDLE_IDENTIFIER"]}.intents"])
